# Optional Wazuh agent integration

1. Update `WAZUH_MANAGER` in `wazuh-agent-daemonset.yaml`.
2. Apply the manifest.
3. Enroll the agents in your external Wazuh manager.

This is intentionally agent-only integration.
