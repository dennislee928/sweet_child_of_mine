apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: wazuh-agent
  namespace: telemetry
spec:
  selector:
    matchLabels:
      app: wazuh-agent
  template:
    metadata:
      labels:
        app: wazuh-agent
    spec:
      hostPID: true
      hostNetwork: true
      containers:
        - name: wazuh-agent
          image: wazuh/wazuh-agent:4.14.0
          env:
            - name: WAZUH_MANAGER
              value: wazuh-manager.example.internal
            - name: WAZUH_AGENT_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          securityContext:
            privileged: true
          volumeMounts:
            - name: host-var-log
              mountPath: /host/var/log
              readOnly: true
            - name: host-proc
              mountPath: /host/proc
              readOnly: true
      volumes:
        - name: host-var-log
          hostPath:
            path: /var/log
        - name: host-proc
          hostPath:
            path: /proc
